# P0: Контрольный прогон после деплоя

**Дата:** _______________  
**Версия:** _______________ (commit hash)  
**Проверяющий:** _______________

---

## Цель

Проверить, что P0-функциональность работает корректно на проде:
1. Успешный writeback с `canonical_url`
2. Safety-кейс: `WP_ROW_NUMBER_MISSING` при отсутствии `row_number`

---

## Таблица для проверки

**Ссылка:** https://docs.google.com/spreadsheets/d/1RJpw2mAMej3a-VC6yKAsKkVQvzGStcjUC7LijNNyn50/edit?pli=1&gid=1039755742#gid=1039755742

---

## Тест 1: Успешный writeback с canonical_url

### Подготовка

1. **Найти или создать тестовую запись в `raw_feed`:**
   - Статус: `READY_TO_PUBLISH`
   - `row_number` заполнен и валиден (>= 2)
   - `published_posts` != TRUE (или пусто)
   - `id` заполнен (для идентификации)
   - `slug` заполнен (или будет сгенерирован)

2. **Записать данные для проверки:**
   - `id`: _______________
   - `row_number`: _______________
   - `slug`: _______________

### Выполнение

**Запустить публикацию:**

```bash
# На сервере
python -c "
from app import create_app
from app.database import db
from app.services.blog.publish import publish_ready_posts

app = create_app('production')
with app.app_context():
    stats = publish_ready_posts(db.session)
    print(f'Stats: {stats}')
"
```

**Или через cron/запланированную задачу** (если настроена автоматическая публикация).

### Проверка результата

**Открыть строку в таблице по `row_number`:**

**Ссылка на строку:** `https://docs.google.com/spreadsheets/d/1RJpw2mAMej3a-VC6yKAsKkVQvzGStcjUC7LijNNyn50/edit?pli=1&gid=1039755742#gid=1039755742&range=ROW_NUMBER:ROW_NUMBER`

**Проверить колонки:**

- [ ] `published_posts` = `TRUE`
- [ ] `published_at` заполнен (ISO 8601 формат, например: `2026-01-28T12:00:00Z`)
- [ ] `canonical_url` = `https://mywavetreaning.ru/blog/{slug}`
  - Проверить, что домен именно `mywavetreaning.ru` (не альтернативный)
- [ ] `publish_error` пуст (или очищен)
- [ ] `publish_attempts` увеличен (было X, стало X+1)
- [ ] `publish_last_try_at` обновлён (ISO 8601 формат)
- [ ] `publish_lock_by` и `publish_lock_until` очищены

### Фиксация результата

**Скриншот "до":** _______________  
**Скриншот "после":** _______________  
**Ссылка на строку:** _______________  
**Результат:** ✅ / ❌  
**Замечания:** _______________

---

## Тест 2: Safety-кейс (WP_ROW_NUMBER_MISSING)

### Подготовка

1. **Создать тестовую запись без `row_number`:**
   - В таблице создать новую строку (или использовать существующую)
   - Заполнить:
     - `id` = уникальный ID (например, `site_p0_test_safety_YYYYMMDD_HHMMSS`)
     - `status` = `READY_TO_PUBLISH`
     - `row_number` = **пусто** или невалидное значение (например, `0`, `-1`, текст)
     - Остальные обязательные поля заполнить (например, `raw_title`, `raw_content`)

2. **Записать данные:**
   - `id`: _______________
   - `row_number`: _______________ (пусто/невалидно)

### Выполнение

**Запустить публикацию** (как в тесте 1).

### Проверка результата

**Открыть строку в таблице по `id`:**

**Ссылка на строку:** _______________

**Проверить, что writeback НЕ выполнен:**

- [ ] `published_posts` остался пустым или `FALSE` (не изменился)
- [ ] `published_at` не заполнен (не изменился)
- [ ] `canonical_url` не заполнен (не изменился)

**Проверить, что ошибка зафиксирована:**

- [ ] `publish_error` = `WP_ROW_NUMBER_MISSING` или `WP_ROW_NUMBER_INVALID`
- [ ] `publish_attempts` увеличен (было X, стало X+1)
- [ ] `publish_last_try_at` обновлён

### Проверка логов

**На сервере:**

```bash
tail -n 200 /var/log/mywave/app.log | grep -A 5 -B 5 "WP_ROW_NUMBER"
```

**Ожидаемые строки:**
```
[blog-publish] row_number отсутствует/невалиден для id=...
[blog-publish] P0-коды ошибок за сессию (всего N): WP_ROW_NUMBER_MISSING: 1
```

### Фиксация результата

**Скриншот строки с ошибкой:** _______________  
**Ссылка на строку:** _______________  
**Выдержка из логов:** _______________  
**Результат:** ✅ / ❌  
**Замечания:** _______________

---

## Проверка логов P0-мониторинга

После обоих тестов проверить логи на наличие P0-статистики:

```bash
tail -n 500 /var/log/mywave/app.log | grep -A 10 "blog-publish.*Статистика"
```

**Ожидаемые строки:**

```
[blog-publish] Статистика: опубликовано=X, ошибок=Y, заблокировано=Z, пропущено=W
[blog-publish] P0-коды ошибок за сессию (всего N): WP_ROW_NUMBER_MISSING: 1
[blog-publish] Топ отсутствующих колонок (WP_SCHEMA_MISMATCH): ...
[blog-publish] Доля успешных ack: X/Y (Z%)
```

**Зафиксировать:**

- [ ] Логи показывают P0-мониторинг
- [ ] Коды ошибок корректны
- [ ] Доля успешных ack рассчитана правильно

---

## Итоговый отчёт

**Дата проверки:** _______________  
**Версия:** _______________  
**Проверяющий:** _______________

### Тест 1: Успешный writeback

- **Строка:** _______________
- **Ссылка:** _______________
- **Результат:** ✅ / ❌
- **canonical_url:** _______________
- **Замечания:** _______________

### Тест 2: WP_ROW_NUMBER_MISSING

- **Строка:** _______________
- **Ссылка:** _______________
- **Результат:** ✅ / ❌
- **publish_error:** _______________
- **Замечания:** _______________

### Логи P0-мониторинга

- **P0-коды ошибок:** _______________
- **Доля успешных ack:** _______________%
- **Замечания:** _______________

---

## Следующие шаги

После успешного контрольного прогона:
- [ ] Зафиксировать результаты в `docs/DECISION_LOG_R2_P0.md`
- [ ] Объявить P0 завершённым
- [ ] Перейти к P1 (CONTRACT-лист)
