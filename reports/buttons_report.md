# Inventory: Buttons and expected behavior

Below is the initial inventory for interactive buttons discovered in `templates/` and `static/` (first pass). Each row contains: button (selector / visible text), template/location, files involved, expected behavior, actual behavior (observed from code / tests), recommended fix (single best project-consistent approach).

| # | Button (selector / text) | Location (template / file) | Files using / handling it | Expected result | Actual result | Recommended fix |
|---|---|---:|---|---|---|---|
| 1 | `#openBookingBtn`, `.book-now` — "Записаться" | `templates/index.html`, `templates/services.html` | `static/js/booking.js`, `app/routes/services.py`, `app/routes/safari.py` | Open booking calendar modal and start booking flow (load slots → select → submit) | `booking.js` attaches handlers to `#openBookingBtn, .book-now` and opens modals; server endpoints exist (`/api/calendar/slots/<date>`, `/api/calendar/book`) and passed through tests for booking flow when monkeypatched. | Keep JS flow; add E2E Playwright tests to verify UX (open modal → select date → submit). Ensure buttons have `data-service` or `data-modal` attributes consistently (some templates use `href="#"` instead). Add accessible attributes (`aria-*`). |
| 2 | Contact submit — "Отправить" | `templates/contact.html` | `app/routes/contact.py`, `app/forms/contact_form.py` | POST contact, save to DB, append to Google Sheets, flash success, redirect to contact | Server logic saves to DB and redirects. Test `test_contact_form_post_with_fallback` verifies DB write when WTForms validator raises (fallback path). | No change required. Keep fallback validator, but render flash messages in templates (verify in `base.html`). Consider adding server-side rate-limit to avoid spam. |
| 3 | Login submit — "Войти" | `templates/login.html` | `app.routes.admin_panel` (route: `admin_panel.login`) | Authenticate user, set session / redirect to admin | Template posts to `admin_panel.login` — handler expected in `app/routes/admin_panel.py` (verify existing). | Add tests for login endpoint (unit/integration). Ensure CSRF token present; tests should assert 200/302 on valid credentials. |
| 4 | Shop filters — `.filter-btn` (Все, Баланс‑борды, etc.) | `templates/shop.html` | `static/js/shop-filter.js` | Filter product cards client-side by category | Works via `shop-filter.js` — buttons toggle `.active` and show/hide cards. | No change required. Add a11y (aria-pressed) and keyboard support. Add E2E tests. |
| 5 | Shop Buy buttons — `.btn.btn-primary-lg` ("Купить") | `templates/shop.html` (each product card) | (no dedicated handler) | Add to cart / open checkout / go to product page | Currently plain `<button>` with no JS handler in repo (no cart endpoint used by these buttons) | Single correct fix: Make buy buttons either anchor links to product detail pages (`/shop/product/<slug>`) or add `data-product-id` and implement a small cart API `/api/cart/add` + client JS. Prefer linking to product detail pages if product catalog is used elsewhere; otherwise implement cart service matching project patterns. |
| 6 | Carousel prev/next — `.carousel-prev`, `.carousel-next` | `templates/index.html`, `templates/services.html` | Inline templates / small JS (if present) | Navigate carousel slides | Buttons exist; carousel JS not obvious in repo; may be powered by small script or CSS | If carousel is required, add a small JS initializer or use existing slider library; otherwise convert to semantic links. Add E2E checks. |
| 7 | `button` in `templates/wakesurf_safari.html` type=submit | `templates/wakesurf_safari.html` | `static/js/booking.js`, `app/routes/safari.py` | Submit booking (safari-specific) → redirect to `/wakesurf-safari/booking-success?id=...` (frontend calls `window.location.href`) | `wakesurf_safari` templates include booking JS which on success navigates to `/wakesurf-safari/booking-success?id=...`. Server-side booking endpoints exist; JS builds success view. | Add E2E test; verify server returns `success_view_url` or JS handles success. Prefer consistent server-generated success URL and/or avoid client trusting server HTML. |
| 8 | `a.btn` anchors (e.g. "Вернуться на главную") | `templates/thank_you.html`, `templates/safari_booking_success.html` | Template only | Navigate to target URL | Anchors correctly reference `url_for` or fixed paths. | No change. Keep as links; ensure relative paths correct. |
| 9 | Reviews submit — `{{ form.submit(class="btn btn-primary") }}` | `templates/reviews.html` | `app.routes.reviews` (handler) | POST review, save and show flash | Template uses WTForms submit; backend expected to save | Add test for review POST; confirm backend handles validation and redirects. |

Notes & next steps

- Automated server-side tests added in `tests/test_buttons_server.py` cover: services pages, contact form fallback, calendar slots and booking flow (with monkeypatches to avoid Google Sheets/Calendar dependency). Those tests passed locally (3 passed).
- Many client-side behaviors (modals, slot selection, success modals, share buttons) require end-to-end browser tests (Playwright or Selenium) to fully validate. I recommend Playwright for speed and maintainability.

If you want, I will:

- Add Playwright end-to-end tests for the booking flow and shop filter + buy button behavior.
- Implement the single recommended fix for the shop "Купить" buttons (either link to product pages or add cart API + client JS) — say which you prefer and I will implement.
- Expand the inventory to include every button found in `templates/` and `static/` (full CSV export).

