[pytest]
minversion = 6.0
addopts = -ra -q
markers =
    integration: integration tests that may require services
    ui: browser-based UI tests (require Playwright)
norecursedirs = .venv venv env build dist node_modules .git
testpaths = tests
python_files = test_*.py
