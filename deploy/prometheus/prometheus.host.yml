# Для Prometheus на том же хосте, что и Gunicorn (без Docker network).
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: mywave-web
    metrics_path: /metrics
    static_configs:
      - targets: ["127.0.0.1:5000"]
